interface:
  display_name: "Monitor"
  short_description: "Monitor child threads with watcher lanes."
  default_prompt: "Use $monitor for the requested snapshot, active-wait, or detached-watch mode. Keep engineering/project/domain leads interruptible and preserve each task's original delivery ownership."
